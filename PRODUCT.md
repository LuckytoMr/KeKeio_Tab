# KeKeIO Tab 产品事实

## 产品定位

KeKeIO Tab 是面向桌面 Chromium 浏览器的新标签页扩展。它以本机配置为基础，帮助用户组织分组与快捷方式、选择搜索和壁纸体验，并在同一账号的多个浏览器安装之间携带允许同步的配置。

产品不是浏览器账号同步的替代品，也不把本机图片、缓存或任意网站数据上传到云端。后端地址固定且受信任，扩展保持零网站访问权限。

## 目标用户与使用环境

- 使用 Chrome、Microsoft Edge 等 Chromium 浏览器的桌面用户。
- 同时使用多个浏览器或多台设备，希望复用新标签页配置的用户。
- 需要明确区分“本机设置”“云端配置”和“手动备份”，并避免误覆盖数据的用户。
- 主要通过鼠标、触控板和键盘在浏览器新标签页中操作；设置界面需要兼顾窄窗口和高缩放场景。

## 核心任务

1. 管理分组和快捷方式。
2. 调整页面外观、布局、搜索和壁纸。
3. 导出、导入和恢复本机配置。
4. 登录账号并同步允许共享的配置。
5. 在本机与云端都存在配置时，安全地选择版本并理解影响。
6. 查看同步状态，知道当前是否已保存、是否离线、是否有冲突以及需要采取什么操作。

## 设置范围

设置体验覆盖六个稳定能力域：

- 外观与布局
- 分组与快捷方式
- 壁纸
- 搜索
- 备份与恢复
- 账号与同步

现有功能必须保留，但可以重新组织信息架构、交互顺序、反馈方式和组件结构。

## 同步边界

允许跨浏览器同步的内容包括分组、快捷方式、搜索设置、内置或远程壁纸选择、轮换设置、遮罩、模糊和主题等共享配置。

本机壁纸文件及其标识、资源缓存、当前浏览分类和轮换历史只保留在当前浏览器，不上传云端。界面必须在用户做决定前清楚说明这一区别。

首次在全新浏览器登录时，如果本机没有有效配置，可以采用云端配置；如果本机和云端都已有配置，必须暂停同步并让用户显式选择。关闭对话框、按 Escape 或选择“稍后决定”不能隐式采用任何一方。

当前持续同步以本机更改上传为主；其他浏览器后续产生的远端更改需要用户明确从后端加载。文案不得暗示现阶段已实现实时双向自动拉取。

## 数据安全原则

- 覆盖、导入、重置和冲突处理必须使用明确动作名称，禁止以“确定 / 取消”表达两个相反的数据选择。
- 用户取消或关闭决策界面时，不得发生数据替换。
- 在替换前展示来源、时间、版本和内容摘要；在可行时提供先导出备份。
- 简单设置可以自动保存，但必须提供“正在保存 / 已保存 / 保存失败”的统一反馈。
- 可逆操作优先提供撤销；持续性问题使用状态横幅，不依赖短暂提示。
- 固定后端地址不作为用户可编辑设置展示。

## 品牌与命名

- 产品名统一使用 `kekeio`；界面标题可以使用 `KeKeIO Tab`。
- 保留现有品牌标识和产品语气，不引入未经确认的新品牌承诺。
- 面向用户的文案使用简体中文，避免暴露 Worker、schema、原子队列等实现术语。

## 设计与工程原则

1. 安静而精确：设置界面信息密集但不拥挤，每个状态和动作都可理解。
2. 本机优先：始终说明操作影响的是当前浏览器、云端还是其他浏览器。
3. 安全可逆：默认动作不覆盖数据，高风险行为需要预览和明确确认。
4. 状态透明：保存、同步、离线、冲突和失败状态在相关任务中持续可见。
5. 一致可访问：键盘焦点、语义、状态播报、触控目标和窄屏布局使用统一规则。

## 事实依据

- 当前实现：`extension/src/newtab/App.tsx`
- 当前样式：`extension/src/styles/app.css`
- 现有设计规范：`DESIGN.md`
- 扩展说明：`extension/README.md`
- 用户提供的设置页与原生确认框截图
- 当前项目的安装、同步、安全和浏览器权限固定约束
