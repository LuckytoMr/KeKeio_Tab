# 安全政策

## 支持范围

安全修复优先应用到 `main` 和最新 Release。`main-latest` 是每次 `main` 验证通过后的滚动构建，不代表长期支持版本。

## 私下报告漏洞

请在 GitHub 仓库的 `Security` 页面选择 `Advisories`，再使用 `Report a vulnerability` 创建私密安全报告。不要为未修复漏洞创建公开 Issue，也不要在报告中附带真实密码、Cookie、Token、私钥、数据库或私人备份。

报告最好包含：

- 受影响的提交、Release 或组件。
- 最小复现步骤和预期安全边界。
- 实际影响与攻击前提。
- 已脱敏的日志、请求或截图。
- 如已准备修复建议，可附最小补丁思路。

维护者会先确认收到报告，再根据可复现性、影响范围和修复复杂度推进。公开披露时间应在修复发布并完成必要升级窗口后共同确认。

## 重点安全边界

- 扩展构建产物保持零网站访问权限。
- 凭据只能发送给固定、受信任的目标服务。
- UHDpaper 后端入口必须保持为固定主机和路径的受限代理。
- 安装与管理入口不得暴露到 WAN 或 Cloudflare Tunnel 公网路径。
- Tunnel Token、SMTP 密码、恢复口令和其他密钥不得进入仓库、日志或公开截图。
